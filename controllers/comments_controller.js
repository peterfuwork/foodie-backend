const User = require('../models/user');
const Comment = require('../models/comment');

module.exports = {
    // createComment(req, res) {
    //     const commentProps = req.body;
    //     User.findOne({ _id: userId})
    //     User.create(commentProps)
    //         .then(comment => res.send(comment))
    //         .catch((err) => {
    //             res.status(422).send({
    //                 message: err.errors
    //             });
    //         });
    // },
};